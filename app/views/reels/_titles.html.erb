<div class="titles">
  <div class="field">
    <%= f.label :title_id %><br />
    <%= f.select :title_id, options_for_select(Title.joins(:abbrv_title).order('abbrv_titles.name').all.map { |t| [  t.abbrv_title.name + " - " + t.lccn.to_s, t.id ] } ), :prompt => "Select a title:" %>
  </div>
</div>
